# Dipasha Medical Store — operations platform

Backend + web console for a retail pharmacy running a quick-commerce style delivery pilot out of Prayagraj. Single store, single warehouse, multi-store-ready schema. Full spec lives outside this repo (the build prompt); this README covers what's actually built. **No native Android/Expo app was built** — every staff-facing flow the spec described for one (rider trips, picker/packer scanning, the daily review alarm) shipped as a web-console equivalent instead, formally descoped at M16 rather than left an open item — see `DECISIONS.md`'s M16 entry.

Built milestone by milestone per the agreed build order — see `DECISIONS.md` for what's been decided at each step, and don't skip ahead of the current milestone.

## Status: M16 — Hardening (error tracking, backups, monitoring)

M13 (Section 10.2: full web read/write parity, plus the SQL console/dashboards and WhatsApp inbound inbox), **M14** (Section 9A.3: chronic patients and refill management), and **M15** (Section 10B: accounting, distributor ordering, e-way bill) are all complete and verified. **M16** (Section 13's final milestone: error handling, backups, monitoring — Play Store packaging formally descoped, see above) is also done — see "Build order" below for what each covered.

What exists right now:
- `apps/web` — a new **Chronic patients** screen (Owner/store_manager): a **Refill due** queue (who's due in the next 7 days, who's overdue and by how many days, a churn-risk flag past a configurable threshold — "the highest-return report in the whole system" per the spec) with one-tap Call, an immediate "Remind via WhatsApp now," and a mark-notified action; a **Patient lookup** tab showing each customer's chronic medications, dose, prescriber, expected exhaustion date, and real purchase history.
- `apps/api` — chronic medications are tracked per (customer, product) pairing. The expected exhaustion date ("30 tablets at one daily is 30 days") is computed from the real sales ledger — seeded immediately from existing purchase history when a flag is created, and recomputed automatically inside every subsequent sale to that customer for that product, never incrementally patched. A WhatsApp refill reminder (a new trigger type on M8's dispatcher, phrased as "your regular item is due," never as medical advice) and an optional standing order (auto-created in the request book each cycle, ready for confirmation, not auto-fulfilled) both fire from the same `setInterval` poller pattern as M8/M13.10, idempotent per exhaustion-date cycle so a fresh purchase re-arms the next one automatically.
- `apps/web` — a new **Financials** screen (Owner-only): a P&L summary for any date range (sales/purchases/gross profit with a cost-unknown warning, expenses by category, a reconciliation guard, a combined payables/receivables dashboard, top products by profit) with a previous-equal-length-period comparison, plus **stock valuation** at cost and at MRP for any date, reconstructed from the movement ledger rather than the live stock view.
- `apps/web` — a new **Accounting** screen (Owner/store_manager): the vendor ledger (balance, ageing, statement, record-a-payment — the payable-side mirror of the customer ledger), an expense log with optional bill photo, a day-book, a cash/bank book, a one-click Tally-compatible CSV export, and an e-way bill utility (check whether one's required, generate the portal-upload JSON, record the number back once issued). **Purchase orders** gained a **Track POs** tab: send a PO over WhatsApp or email, mark it acknowledged, a chase list for anything unacknowledged past a configurable window, and ordered-versus-received/billed per line, with PDF/CSV export.
- `apps/api` — the accounting layer (Section 10B): a vendor ledger, expenses, day-book, cash/bank book, management P&L, and stock valuation, deliberately *not* a Tally replacement (no journal vouchers, no trial balance) — plus PO send-and-track and an e-way bill/e-invoicing stub. An optional Owner-only daily WhatsApp digest (four headline figures, off by default) shares M8's dispatcher and the same `setInterval`-poller pattern as every other scheduled feature. Live reconciliation-guard testing against real seeded data caught and fixed two real bugs from earlier milestones: `sale_tenders.amount` for a cash tender is the amount handed over, not net revenue, and `sales.change_due` had never been subtracted anywhere — including M4's own day-close "expected cash" preview — overstating cash by the total change given on every cash sale; and a Postgres enum/`text` type mismatch that broke the cash/bank book query outright. Both are fixed and documented in `DECISIONS.md`.
- `apps/web` — **F3 item details** (owner-requested, not from the build spec): press F3 on a medicine in the unified search bar, or click "Details" on any result, to see every product-master field — pack size, composition, strip-to-base-unit conversion, GST, rack locations, and more — with an Edit button (Owner/store_manager) to change and save any of it in place. Viewing is open to every role; `PATCH /products/:id` was widened from its original 6 editable fields to the full product-master field set, and editing composition or form now correctly recomputes the substitute group.
- `apps/web` — **F4 item ledger** (owner-requested, not from the build spec): press F4 on a medicine in the unified search bar (POS, Request book, Purchase entry, Delivery order entry — wherever the search bar is used), or click "History" on any result, to see that item's full sales and purchase history at date level. Clicking any history row opens the real original document — the printed bill or the GST purchase invoice — in a new tab. Owner/store_manager only, same visibility bar as the sales/purchase-invoice list screens.
- `apps/web` — a new **WhatsApp inbox** screen (Owner/store_manager): every inbound WhatsApp message, filterable by unhandled/all, showing the matched customer/order where one exists, a reply box, and a mark-handled button.
- `apps/api` — a new unauthenticated `/whatsapp/inbound` webhook, built to Meta's real WhatsApp Cloud API contract (GET handshake against a settings-table verify token, POST body shaped like `entry[].changes[].value.messages[]`). An inbound message is matched to an existing customer by phone (never auto-creates one) and, if that customer has an order in flight, mirrors into that order's `order_messages` thread. "STOP" and its common variants (exact match, never a substring) automatically opt the customer out via M8's existing consent mechanism. A reply from the inbox is free text — the one WhatsApp trigger type in this build that isn't a Meta-approved template, since WhatsApp allows a free-form reply within 24 hours of a customer's own message — routed through M8's single dispatcher as a new trigger type.
- `apps/web` — **Reports** has a **Dashboard** tab (the default) with live operational stat tiles, an Owner-only **SQL console** (read-only, run any `SELECT`), and a **Daily reports** tab listing frozen once-a-day snapshots.
- `apps/api` — the SQL console is read-only in two independent layers: a text check, and the query actually runs inside a Postgres `SET TRANSACTION READ ONLY` transaction that the database itself enforces regardless of how a write is disguised (verified live against a data-modifying CTE). The daily auto-report is a plain `setInterval` poller (M8's WhatsApp-dispatcher pattern, not a new scheduling mechanism) that snapshots the same summary the dashboard shows live, once per business date, and sends it to the Owner over the existing WhatsApp dispatcher as a new trigger type.
- `apps/web` — a new **Settings** screen (Owner-only): every configurable threshold this build has seeded since M1 — 55 of them by now, across nearly every milestone — in one place, grouped, editable inline, taking effect immediately. No migration was needed; the `settings` table's `updated_at`/`updated_by` columns have existed unused since M1 for exactly this.
- `apps/api` — `PATCH /settings/:key` invalidates that key's in-process cache immediately (no longer waiting out the existing 30-second TTL) and rejects a value whose JSON type doesn't match what's already there, so a mistaken edit fails at the point of the mistake instead of surfacing later as a confusing bug wherever that setting is actually read.
- `apps/web` — **Cycle counts** gained a **Schedule & assign** tab: pick specific bins for any date (today or upcoming) regardless of what the daily automatic selection would pick, and assign a counter directly. The "Today" view also gained an inline assign dropdown on every pending task — the backend has supported assignment since M6, it simply had no UI until now. A new **Cold chain** screen logs temperature readings with a gap/out-of-range banner (`Section 9`) — genuinely new, no temperature-tracking table existed anywhere before this.
- `apps/api` — cycle-count scheduling reuses the `manual` value the `cycle_count_selection_reason` enum has carried since M6, unused until now. Cold-chain gap-checking is computed live on request (no background worker), same pattern as M5's daily-request-review check.
- `apps/web` — **Purchase entry** and **Stock received** each gained a list tab (**Invoices** / **Received log** — filterable, previously create-only with zero read path). The invoice detail screen shows lines, lets a scanned/photographed copy of the physical invoice be attached after the fact, and supports correcting the invoice number/date/vendor/payment-terms header fields (never quantity, rate, or GST — those already feed posted stock movements). **Put-away** gained a "Quantity found" field on every confirmation for a fresh receipt — if it doesn't match what was invoiced or entered, a required reason + note logs a receiving variance, and a new **Receiving variances** tab lists open/resolved ones for a human decision.
- `apps/api` — the receiving-count variance step closes a gap flagged back in M3 ("no separate physical-count-at-receipt step distinct from data entry yet"): put-away now moves the *actually found* quantity into the destination bin, not the originally-recorded one, with the staging bin trued up by an adjustment entry. Invoice document upload reuses Section 6.3's AI-scan file-storage helpers unchanged.
- `apps/web` — **Delivery orders** gained an **All orders** tab (status/date-range/search filters, CSV export) and, on the order review screen, an **Order actions** panel: **Cancel order** (reason code + required note, blocked once already terminal or already dispatched — a dispatched order points staff at the Rider screen's delivery-failure flow instead), **Force-reassign rider** (rider picker + required note, shown only while `assigned`/`out_for_delivery`, explicit that it only corrects the system record), and a **refund-initiation stub** (amount + reason, clearly labelled as recording intent only — no payment gateway). Pre-pick order editing (add a catalogue item, remove a line, change quantity, substitute) now works across the *whole* pre-pick window, not just the first two statuses.
- `apps/api` — order cancellation needed zero new stock-reversal logic: stock is never deducted until pack time, so only `packed`/`partially_available` orders (which already have a `sale_id`) reuse the existing `cancelSale` (Section 6A.7, day-close-blocked, full reversal); every earlier status is a pure status flip. Force-reassign is a real audit trail (`order_reassignments`) with a required note rather than a reason-code enum, same character as M13.4/M13.5's judgment-call logs. Refunds (`order_refunds`) start and stay at `requested` — there's nowhere else for them to go until a real payment gateway exists.
- `apps/web` — **Bins** gained a **Rack map** tab: a real grid of aisles/bays/shelf levels, each bin coloured by how full it is (against its own capacity score) with its stock value shown, and drag-one-bin-onto-another to reslot — which queues a put-away task exactly like the Inventory screen's Move stock, never a silent record change. The list view gained **Rename**, **Retire** (blocked with a clear error if the bin still holds stock), and **Merge into…** (queues a move task for everything currently in the bin; the source bin can only actually be retired once every task is scan-confirmed).
- `apps/api` — retiring a bin now hard-blocks while it holds recorded stock, the same character as the cold-chain/SH1 zone-forcing rule — a physical-consistency issue, not a judgment call. Merge and drag-to-reslot are both built entirely on M3's put-away pipeline and M13.3's Inventory move-stock endpoint — no new stock-movement code in this milestone at all. A real gap fixed in passing: bins created via the web form (code only) never had their aisle/bay/shelf parsed out even when the code structurally encoded them, which would have made them invisible to the rack map — fixed for every future bin regardless of how it's created.
- `apps/web` — **Products** has substitute-group management, bulk CSV import with preview-diff, and barcode label-sheet PDFs (a genuine gap — only bins ever had one before).
- `apps/web` — a new **Inventory** screen: full filterable stock view, quantity/batch/expiry/MRP correction with an audit row, block/unblock a batch, Move stock (**the bin-to-bin migration you asked about earlier, deferred from M11**), and bulk CSV import.
- `apps/api` / `apps/web` — the five scan-backed actions Section 10.1 names all require a mandatory reason code + note when done from web. A **Manual overrides** tab on Reports lists every one.
- `apps/web` — a **Staff** screen (owner-only): full account CRUD, permission overrides, rider onboarding, roster, activity log.
- **M16 hardening** — real Sentry error tracking wired into the API (Fastify `setErrorHandler`, global `uncaughtException`/`unhandledRejection` handlers) and the web console (`Sentry.ErrorBoundary`), a complete no-op until `SENTRY_DSN`/`VITE_SENTRY_DSN` are set; automated daily backups (`scripts/backup.sh` — Postgres dump + uploads archive, 30-day retention, an off-server `rclone` destination once real object storage exists) with a tested, documented restore procedure (`scripts/restore.sh`); uptime monitoring (`scripts/uptime-check.sh`, alerts over WhatsApp only on a state change) and a weekly backup-freshness check (`scripts/backup-freshness-check.sh`), both with an honest log-fallback until real WhatsApp Business API credentials exist. **The native Android/Expo app is formally descoped** — see the note at the top of this file and `DECISIONS.md`'s M16 entry.
- **Purchase entry / Accounting** (owner-requested, post-M16, from a real vendor bill audit) — Purchase entry gained an optional **invoice time** field and a per-line **Cess ₹** input (both already existed end-to-end in the schema/API; only the UI box was missing), plus a collapsible **scheme promised qty** section per line so the M7 margin report's scheme-shortfall section finally has real data to read. The vendor ledger on **Accounting** gained a **bank account** section (bank name/account number/IFSC) and a **Debit notes** section — record goods physically returned to a vendor (damaged/expired/wrong item/short supply) against any of that vendor's purchase invoices, picking lines/quantities/bin per return; it reverses real stock out of the named bin (reusing the `purchase_return` movement type, reserved unused since M1) and reduces vendor payable everywhere it's computed (balance, ageing, payment allocation, statement).
- **Inventory** (owner-requested, post-M16) is now also open to **Picker/packer**, not just Owner/store_manager — the per-row Edit modal (quantity +/-, batch/expiry/MRP correction, block/unblock, every one reason-and-note required) is exactly what was asked for; "Move stock" and "Bulk import" stay Owner/store_manager-only. No new "auditor" role was created — this is a widened grant on an existing screen for an existing role.
- **Activity logs** (owner-requested, post-M16) — a new **Activity logs** tab, Owner-only: a single, plain-English, filterable feed (date range, category, staff member) of every bill generated/credited/cancelled, every purchase recorded or corrected, every stock quantity or price change, every substitute-group change, and every completed cycle count — with who did it, when, and (for corrections) what changed from what to what. Click a row to open the real bill, purchase invoice, or item history behind it. Built from the same real audit trails that already existed (batch corrections, purchase-invoice corrections, the movement ledger, credit notes, cycle counts) rather than a second logging mechanism. The F4 item-history modal also gained a **Changes** section (Owner/store_manager) showing the same detail scoped to one medicine — go to a product and see exactly who increased/decreased its stock or changed its price, and why. Distinct from M13.1's existing Staff → "Activity log" tab, which stays a bare HTTP-call audit (who hit which endpoint) with no entity detail — this one answers "what actually happened."
- **Home dashboard** (owner-requested, post-M16, modelled on a competitor app screenshot) — a new **Home** tab, Owner-only, and the console's default landing page for that role. Net sales/purchases with a sparkline and previous-period comparison, stock value, a customer donut (new vs. repeat, with total customers and average order value), net payment collection, profit, a Due Payments panel (Customer vs. Distributor tabs, each with a "remind now"/record-payment path), Refill reminders, and Slow/Fast moving product tables — one consolidated `GET /owner-dashboard` call, reusing the same P&L/stock-valuation/cash-book/refill-due functions the rest of the console already trusts. Never shown to store_manager or any other role.
- **POS billing** (owner-requested, post-M16) no longer sends a WhatsApp bill notification automatically on sale completion — the post-save panel's existing **Print receipt** / **Send via WhatsApp** buttons are now the only way a bill notification goes out, a deliberate biller choice rather than a silent side effect of saving the bill.
- **Customers screen** (owner-requested, post-M16) gained a **Bulk import** tab — CSV upload of a customer list with the same mandatory preview-and-confirm diff as the product master's bulk import, matched by phone. The existing **Customer ledger**'s Statement gained an **"All time"** quick range and clickable bill rows — click any bill to open the real original invoice (every medicine, batch, quantity, GST breakdown) in a new window, the same document F4's item history already opens. The page (and its search, which already covered every customer, not just credit ones) was renamed from "Credit customers" to "Customers" to match what it's always actually done.
- Everything from M2–M12: product/bin/vendor masters, GST purchase entry, put-away, POS billing with FEFO (online and offline-first with sync-on-reconnect), sale returns, day-close, customer request book, purchase orders, callback loop, daily review alarm, cycle counts, expiry audit, damage/write-off with photo evidence, Section 10A statutory reports, prescriber master, margin/scheme reporting, credit customers with ageing, pack-aware reorder, vendor comparison, the Section 12A WhatsApp notification dispatcher, AI invoice scanning, the delivery channel (order entry, pick list, packing verify, Section 7A unstructured intake), and the rider/dispatch module (handover scan, delivery marking, COD reconciliation, failed-delivery returns)
- Full data model: settings, users/roles, salt master, product master + composition child table, bin master, batches, search log, vendors, purchase invoices + lines, put-away tasks, sales + lines + tenders + prescriber details, credit notes, day-close, cycle counts, write-offs, `notification_log` (WhatsApp queue + send log), `purchase_invoice_scans` + `purchase_invoice_scan_pages` + `vendor_product_aliases` (AI invoice scanning), `orders` + `order_lines` + `order_images` + `order_pick_lines` + `delivery_batches` + `order_messages` (delivery channel), `order_gps_pings` + `delivery_return_tasks` + `rider_cash_reconciliations` (rider/dispatch), `bill_number_blocks` + `sync_conflicts` (offline sync), `permission_overrides` + `rider_details` + `rider_documents` + `activity_log` + `user_last_seen` (staff & roles), `web_manual_overrides` (manual-override reasons for actions with no ledger row of their own), `batch_corrections` (non-quantity stock-edit audit trail), `product_group_changes` (substitute-group override audit trail), and the append-only movement ledger (all 9 `movement_type` values, DB-trigger-enforced immutability, on-hand stock as a derived `VIEW`, never a stored column)
- Seed script: 4 role users, 25 salts, 50 dummy SKUs (auto-grouped into real substitute groups), 59 bins across every zone prefix, batches with opening stock
- `packages/theme` — shared design tokens (estimated from the real logo/site screenshot — see `packages/theme/README.md`)
- Docker Compose: Postgres + API + web (nginx) + Caddy (automatic HTTPS, path-split `/api/*` vs the console)
- CI: typecheck, build, and a Docker smoke test for both the api and web images on every push
- `deploy.sh` + `RUNBOOK.md`: how to actually get this running on a VPS

## Repo layout

```
apps/
  api/            Fastify backend
    migrations/   node-pg-migrate files (schema, in order)
    scripts/seed.ts  Dev seed data — 50 SKUs, bins, batches, opening stock
    src/domain/   Business rules shared across routes/seed (e.g. substitute grouping)
    MIGRATIONS.md How the migration/seed workflow works
  web/            React + Vite owner/staff console
packages/
  theme/          Shared design tokens (colors, spacing) — web + app both import this
infra/
  Caddyfile       Reverse proxy config (templated with $DOMAIN, splits /api/* from the console)
docker-compose.yml
deploy.sh         Deploy script for the VPS
RUNBOOK.md        How to deploy and operate this, without needing me
DECISIONS.md      Every business rule and stack choice, and why
```

## Local development

```
cp .env.example .env          # set POSTGRES_PASSWORD and JWT_SECRET (openssl rand -hex 32)
docker compose up -d postgres
npm install
npm run migrate:up --workspace apps/api
npm run seed --workspace apps/api   # optional: 50 dummy SKUs to test against
npm run dev:api                     # terminal 1 — API on :3000
npm run dev --workspace apps/web    # terminal 2 — console on :5173, proxies /api to :3000
```

Open http://localhost:5173. Log in as the seeded owner with phone `+919999900001` — in development the OTP screen shows the code directly (no real SMS provider is wired up yet). PIN for all seeded users is `1234`.

## Full stack via Docker

```
cp .env.example .env   # set POSTGRES_PASSWORD and JWT_SECRET at minimum
docker compose up -d --build
docker compose exec api npm run migrate:up --workspace apps/api
curl http://localhost:3000/health   # api
curl http://localhost:8080/         # web console
```

See `RUNBOOK.md` for deploying to a real VPS with HTTPS on a subdomain.

## Build order

Milestones M0–M16 as agreed. Each one gets built, tested by the owner, then the next starts — no skipping ahead, no scope creep beyond what's specified. **M13** (Section 10.2's full web read/write parity, the SQL console/dashboards, and the WhatsApp inbound inbox) is far larger than any milestone before it, so it's being built and pushed as a sequence of chunks rather than one pass. **M13.1 (Staff & roles)** is done — full account CRUD, permission overrides enforced by extending `requireRole` itself, rider onboarding, a per-user activity log. **M13.2 (manual-override infrastructure)** is done — mandatory reason codes for pick/pack/handover/cycle-count entry on web, a new Manual Override report. **M13.3 (Inventory)** is done — full filterable stock view, quantity/batch/expiry/MRP correction with an audit row, block/unblock a batch, move-stock-between-bins reusing M3's put-away pipeline (**the bin-to-bin migration explicitly deferred from M11**), three bulk CSV imports with preview-diff. **M13.4 (Product master)** is done — substitute-group management (a straight `UPDATE` on the join key every consumer already reads, no override path existed before), bulk product CSV import, and product barcode label-sheet PDFs (a genuine gap — only bins ever had one). **M13.5 (Bin master)** is also done — rename and retire were already technically possible since M2 (`code`/`status` were always updatable), but retiring a bin with recorded stock had no safety check; it's now a hard block, same character as the cold-chain zone-forcing rule. "Merge" queues a put-away task per item in the source bin rather than combining records directly, which composes naturally with the new retire-block: a merged-from bin can't actually be retired until every queued task is scan-confirmed. The visual rack map (aisle/bay/shelf grid, fill % against each bin's existing `capacity_score`, value heat) and drag-to-reslot are built entirely on M13.3's Inventory move-stock endpoint — no new stock-movement code. A real gap fixed in passing: bins created via the web form never had aisle/bay/shelf parsed from their code even when it structurally encoded them, which would have made them invisible to the rack map. Verified end to end against the real running API: retiring a stocked bin correctly 409s with the exact quantity; merging one bin into another queued a real put-away task, left the source bin blocked from retirement until the task was scan-confirmed, then retirement succeeded; the rack map correctly showed real fill/value data pulled from live stock, including one bin honestly displaying over 100% fill rather than clamping the number. **M13.6 (Orders)** is also done — manual creation and the full filterable/exportable order list reused M10's existing order-entry form and a new `GET /orders` (CSV via the same `toCsv` helper Section 10A's reports use); edit-pre-pick (add/remove/change-quantity/substitute) needed only two new endpoints, since quantity-change and substitution both reuse M10's existing line-resolve endpoint once its web-side gating was widened to the full pre-pick window. Cancel-with-reversal needed zero new stock-reversal logic — stock is never deducted until pack time, so only `packed`/`partially_available` orders (already carrying a `sale_id`) reuse the existing `cancelSale` from M4/M13.3; every earlier status is a pure status flip. Force-reassign-rider is a real audit trail with a required note (same character as M13.4/M13.5's judgment-call logs), explicitly limited to `assigned`/`out_for_delivery` and explicit that it only corrects the system record. Refund initiation is a deliberately inert stub per Section 10.2's own wording — records intent, never fakes a "processed" state. Verified end to end against the real running API: a pre-pick order edited (line added, then removed) via the catalogue search; a `received`-stage order cancelled with zero ledger activity; a freshly packed order (real sale, open business day) cancelled with the batch's stock correctly reversed back to its exact pre-sale figure; a cancel against a closed-business-day sale correctly 409ing via the reused guard rail; a dispatched order's rider force-reassigned with the audit row written and a same-rider no-op correctly rejected. Live verification also caught and fixed a real bug — the order-actions panel was hiding a recorded refund the moment its order reached a terminal status, since the whole panel (not just cancel/reassign) disappeared once terminal; refunds now stay visible regardless of order status. **M13.7 (Inbound/GRN)** is also done — `GET /purchase-invoices` (list + detail) and `GET /stock-movements/stock-received` close a real gap: both records have been create-only since M3, with no read path at all beyond the database itself. "Edit inbound records" is deliberately narrow — a new `purchase_invoice_corrections` table (shaped like M13.3's `batch_corrections`) only allows correcting invoice number/date/vendor/payment-terms, never quantity/rate/GST, which already feed posted stock movements; a wrong MRP is still fixable through the existing Inventory-screen batch-correction tool regardless of how the batch arrived. Invoice document upload reuses Section 6.3's AI-scan file-storage helpers unchanged. The receiving-count variance step closes a gap flagged explicitly back in M3 ("no separate physical-count-at-receipt step distinct from data entry yet") — put-away confirmation now accepts the quantity actually found, and when it differs from what was invoiced or entered, logs a required-reason variance and moves the *physically found* quantity into the destination bin, not the recorded one, truing up the staging bin with an adjustment entry. Verified end to end against the real running API: invoiced 10 units, found 8 at put-away — staging correctly zeroed out and the destination bin received exactly 8; an internal bin-transfer task correctly ignored a differing "quantity found" override since it isn't a fresh receipt; a variance resolved with a required note, a second resolve attempt correctly rejected; an uploaded invoice PDF fetched back byte-for-byte identical; a header correction (typo'd invoice number) recorded with full old/new audit history. **M13.8 (Audits)** is also done — cycle-count scheduling needed almost no new schema, since M6 had already carried an unused `manual` value on the selection-reason enum for exactly this; the bigger surprise was that assignment (`PATCH /cycle-counts/:id/assign`) had existed since M6 with zero UI anywhere until now. Cold-chain temperature logging is genuinely new — no such table existed before this build — with `in_range` frozen at write time against the settings in force then, and a gap-check computed live on request rather than by a background worker (same pattern as M5's daily-request-review alarm). One shop-wide reading stream, not per-fridge, since no equipment model exists anywhere in this schema — flagged for the Owner if that's wrong. Verified end to end: two bins scheduled for tomorrow, a repeat schedule call against the same date correctly skipped both, listing by an arbitrary date worked, and a scheduled task was assigned to a picker/packer; an in-range and an out-of-range temperature reading both logged correctly, and the gap-check correctly reported "never logged" before the first reading existed and "last reading out of range" after the second. **M13.9 (Settings)** is also done — needed no migration at all, since the `settings` table's `updated_at`/`updated_by` columns have sat unused since M1 waiting for exactly this; `GET/PATCH /settings` is read/write parity for a table every prior milestone already wrote defaults into (55 rows by now), not a new mechanism. Edits invalidate that key's cache immediately rather than waiting out the existing 30-second TTL, and a value can't silently change JSON type (a number can't become a string) since that would surface later as a confusing bug wherever the setting is actually read, not at the point of the mistake. Grouping on the screen is a client-side prefix match, not a stored category — no schema needed for pure display grouping. Verified end to end: edited a threshold through the real browser UI and confirmed the new value took effect on the very next request from an unrelated endpoint (no stale read), a type-mismatched edit correctly 409s, an unknown key correctly 409s. Looking at every setting in one place for the first time also surfaced a real inconsistency worth flagging rather than silently fixing: three pairs of settings (`order_batch_max_orders`/`delivery_batch_cap`, `order_batch_window_minutes`/`delivery_batch_window_minutes`, `pending_order_response_target_minutes`/`order_response_target_minutes`) look like the same threshold seeded twice — an early M1 placeholder and M10's later real-feature seed, neither ever removed. **M13.10 (Reporting/query layer)** is also done — the SQL console is read-only in two independent layers (a text check, plus the query itself running inside a Postgres `SET TRANSACTION READ ONLY` transaction the database enforces regardless of how a write is disguised — verified live against a data-modifying CTE that passed the text check but was still blocked by Postgres, with a 5-second statement timeout and 500-row cap rounding out the defenses), Owner-only since this is raw database access even though read-only. The prebuilt dashboard and the daily auto-report share one summary function rather than two aggregation paths that could drift apart; the daily report freezes its result once per business date (same snapshot-at-the-meaningful-moment reasoning as cycle counts and batch corrections) via a plain `setInterval` poller — no background job runner exists in this build, so this follows M8's own WhatsApp-dispatcher precedent rather than inventing a second scheduling mechanism — and sends the summary to the Owner as a new trigger type on M8's existing dispatcher, not a second notification mechanism. Verified end to end: valid SELECT and WITH-SELECT queries returned real data; a plain DELETE was rejected before touching the database; a disguised data-modifying CTE was rejected by Postgres itself; a slow query hit the statement timeout; and — waiting for a real poller tick, not simulated — the daily report generated, froze a `daily_reports` row with live numbers, and sent the WhatsApp summary. **M13.11 (WhatsApp inbound shared inbox)** is also done — the last chunk of M13, closing two gaps explicitly left open for it: M10's `order_messages` table (no inbound channel existed yet, so `sender = 'customer'` rows were staff transcribing a phone call) and M8's `updateWhatsAppConsent` (no webhook existed yet to auto-process a customer's own STOP reply). The webhook is built to Meta's real WhatsApp Cloud API contract even though the send side is still stubbed — GET handshake against a settings-table verify token, POST body shaped exactly like `entry[].changes[].value.messages[]`, always returning 200 so Meta doesn't disable it. An inbound message is matched to an existing customer by phone (never auto-creates one) and, if that customer has an order still in flight, mirrors into that order's existing conversation thread; an unmatched number still lands in the inbox for a human to triage, which is the actual point of a shared inbox. "STOP"/"unsubscribe"/"cancel"/etc. are matched exactly, never as a substring, so "please stop by tomorrow" doesn't trip an accidental opt-out. A reply from the inbox is the one WhatsApp trigger type in this build that's free text rather than a Meta-approved template — allowed under WhatsApp's own 24-hour-reply-window rule — but still goes through M8's single dispatcher, needing no migration since `trigger_type` was always unconstrained text. Verified end to end, via curl then via the new Shared Inbox screen in the browser: the handshake token-checks correctly; an inbound message matched both customer and order and appeared on the order's thread; a reply sent from the UI was logged by the dev WhatsApp sender, auto-marked the message handled, and appeared on the order thread as a staff message; a STOP message flipped both consent flags while a near-miss phrase didn't; replying to an already-opted-out customer correctly logged `skipped_opted_out` while still marking the message handled; an unmatched number showed in the inbox with no customer/order badge and could be marked handled directly. **M13 is now fully complete across all 11 sub-chunks.** **M14 (Chronic patients and refill management, Section 9A.3)** is also done — the tracked entity is a (customer, product) pairing, not a bare product flag, since every capability the spec asks for (expected exhaustion date, refill-due list, churn signal, patient profile) needs a specific customer's dosing and purchase history. The expected exhaustion date ("30 tablets at one daily is 30 days") is computed from the real sales ledger, never incrementally patched — seeded immediately from existing purchase history the moment a flag is created, and recomputed inside `createSale`'s own transaction on every later matching sale. The refill-due list and the churn-risk flag share one screen, since a staff member working the queue needs both together; the churn threshold is a live setting, so changing it re-flags rows on the next load with no migration. The WhatsApp reminder ("your regular item is due," never phrased as medical advice, per the spec's own explicit rule) is a new trigger type on M8's dispatcher; the optional standing order auto-creates a request-book entry each cycle, ready for confirmation, attributed to a new `movement_source` value (`'automated'`) rather than mislabeled as a human action. Both the automated poller and a manual "Remind now" button on the refill-due screen share one code path, idempotent per exhaustion-date cycle so a fresh purchase re-arms the next reminder automatically. Verified end to end, including a real unattended poller tick (not simulated): a customer's real purchase history correctly seeded an overdue exhaustion date; the churn-risk flag correctly reacted live to a settings change; a fresh sale correctly recomputed the exhaustion date forward; the manual reminder button sent the real WhatsApp text and auto-created the standing order; and, five minutes later with zero intervention, the real background poller independently processed a second due row and correctly produced a `skipped_opted_out` result for an opted-out customer — the same consent gate M13.11 already relies on, composing correctly with no special-casing. **M15 (Section 10B: Accounting, distributor ordering, and e-way bill)** is also done — the "books-quality data layer, not a Tally replacement" the spec explicitly asks for: a vendor ledger (the payable-side mirror of M7's customer ledger), expenses, a day-book, a cash/bank book, a management P&L with a payables/receivables dashboard and a reconciliation guard, stock valuation for any date reconstructed from the movement ledger, a Tally-compatible CSV export (not XML — nothing here can validate a hand-rolled Tally schema against a real Tally instance), PO send-and-track widened onto M5's `purchase_orders` (ordered-versus-received/billed collapsed to one number, since this build's real GRN flow has no separate pre-invoice receiving stage — a GST purchase invoice's creation *is* the goods-in event), an e-way bill/e-invoicing stub (genuinely inert by the spec's own instruction — generates the NIC-portal-shaped upload JSON, submits nothing), and an optional Owner-only daily WhatsApp digest of the four headline figures, off by default, on the same `setInterval`-poller pattern as every other scheduled feature in this build. Live reconciliation-guard testing against real seeded data — not just typecheck — caught and fixed two real bugs reaching back to M4: `sale_tenders.amount` for a cash tender is the amount physically handed over, not net revenue, and `sales.change_due` (the change given back) had never been subtracted anywhere it should have been, including M4's own day-close "expected cash" preview, overstating cash by the total change given on every cash sale with change; and a Postgres enum/`text` type mismatch that broke the cash/bank book query outright. Both are fixed everywhere they occurred, including the older M4 code, and documented in full in `DECISIONS.md` rather than silently patched. New web: a Financials screen (Owner-only — P&L, stock valuation, previous-period comparison) and an Accounting screen (Owner/store_manager — vendor ledger, expenses, day-book, cash/bank book, Tally export, e-way bills), plus a Track POs tab on the existing Purchase orders screen. Verified end to end against a rebuilt, restarted API server: the reconciliation guard, cash/bank book, vendor ageing, and PO send/acknowledge/export endpoints all exercised with real data and real WhatsApp dev-sender output; the financial digest poller invoked directly and confirmed idempotent after stamping its last-sent date. **M16 (Hardening: error handling, backups, monitoring)** is also done — the final milestone in Section 13's build order, with one real fork worth calling out: M16 is spec'd assuming a native Android/Expo app exists (for the Play Store internal-testing half), but no such app was ever scaffolded — every milestone since M5 substituted a web-only equivalent instead, each time flagged as a known, disclosed gap. Put directly to the owner rather than assumed either way: **formally descope the native app**, ship web-only hardening, and treat this as the final architecture, not an open item. What got built: real Sentry SDKs in both apps (`apps/api/src/lib/error-tracking.ts`, `apps/web/src/lib/error-tracking.ts`), a total no-op until a DSN is set, same honest-fallback shape as every other unconfigured integration in this build; a new Fastify `setErrorHandler` that reports genuine 5xxs to Sentry and hides internals from the client outside development, without changing behavior for any of the deliberate 400/404/409s routes already return (every custom error class already followed a catch-by-`instanceof`-or-rethrow pattern, so this only ever catches the rethrow case); `scripts/backup.sh`/`restore.sh` (Postgres dump + uploads archive, 30-day retention, off-server via a configurable `rclone` remote) and `scripts/uptime-check.sh`/`backup-freshness-check.sh` (WhatsApp alerts on state change, via a minimal standalone sender separate from the app's own dispatcher, since these have to work when the API itself is down). A real, live-only-catchable bug came out of actually testing this rather than trusting the scripts once written: a `set -e` gotcha in `uptime-check.sh` (and the same pattern in `backup-freshness-check.sh`) where a real connection failure/missing directory silently killed the alert script before it could ever alert — found via `bash -x`, fixed, and re-verified with an actual simulated outage. Verified end to end: a real backup ran against the live dev database and a real restore into a scratch database matched source row counts exactly across four tables; a simulated missing/zero-byte/stale backup each correctly triggered the freshness alert; a real kill-and-restart of the API produced exactly one DOWN alert and exactly one RECOVERY alert, no duplicates, no false alert on the very first run; the new error handler was verified to hide details in production and show them in development via a temporary throwing route, added and removed for the test. None of this needed Docker to test, deliberately, since the scripts had to work the same way in this build's own Docker-less verification sandbox as they will on the real VPS.
